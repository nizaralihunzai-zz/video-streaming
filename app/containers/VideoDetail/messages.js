/*
 * VideoDetail Messages
 *
 * This contains all the text for the VideoDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.VideoDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VideoDetail container!',
  },
});
