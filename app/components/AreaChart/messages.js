/*
 * AreaChart Messages
 *
 * This contains all the text for the AreaChart component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AreaChart';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AreaChart component!',
  },
});
